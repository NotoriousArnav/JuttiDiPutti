[project]
name = "ohnoooooo"
version = "0.1.0"
description = "Avoid getting Caught!"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame>=2.6.1",
    "pyqt6>=6.11.0",
    "pillow>=10.0.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
]
